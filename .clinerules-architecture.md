# SC-TCG Project Architecture (Final)

## Directory Structure
```
z:/Travail/Dev/SC-TCG/20260601/
└── src/                 # Core domain layer (vanilla TypeScript)
    └── index.html      # Root HTML template
    └── main.ts         # Entry point with minimal DOM interactions
└── CARDS/
    └── TDM.json        # Card data source
    └── sc.json         # Scars of Miracula set data
└── tsconfig.json       # Strict TypeScript config
└── test/unit/          # Vitest unit tests (pure TS, no DOM)
└── test/e2e/           # Playwright BDD scenarios